# Day 09 Notes

## Problem added

- `1431. Kids With the Greatest Number of Candies` (Java) — Find max in array, then check each kid `candies[i] + extraCandies >= max`, build `List<Boolean>`.

## Complexity

- Time: O(n) — two passes over `candies`
- Space: O(n) — result list (output size)
