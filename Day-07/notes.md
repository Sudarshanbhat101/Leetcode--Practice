# Day 07 Notes

## Problem added

- `48. Rotate Image` (Java) — Transpose the matrix (swap across diagonal), then reverse each row. In-place 90° clockwise rotation for n×n matrix.

## Complexity

- Time: O(n²)
- Space: O(1) extra (only a few `temp` variables)
