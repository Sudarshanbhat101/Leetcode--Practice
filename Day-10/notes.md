# Day 10 Notes

## Problem added

- `13. Roman to Integer` (Java) — Scan right to left; if current symbol value is less than previous, subtract (e.g. `IV`); else add.

## Complexity

- Time: O(n), n = length of string
- Space: O(1)
