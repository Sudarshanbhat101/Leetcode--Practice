# Day 04 Notes

## Problems added

- `2553. Separate the Digits in an Array` (Java) — For each number, count digits, extract digits into a small array, append in left-to-right order to a list, then copy to result array.
- `136. Single Number` (C++) — XOR all elements; pairs cancel (`a ^ a == 0`), leaving the unique value.

## Tips

- XOR trick for “one unique, rest pairs” is a classic bit pattern to remember.
- Mixed language per problem is fine if you solved one in Java and one in C++ on LeetCode.
